# Contributing

Thank you for considering contributing!

## Development Setup
1. Clone the repo
2. Install latest Android Studio + NDK
3. Run `./gradlew assembleDebug`

## Branching
- `main`: stable
- Feature branches: `feat/<short-description>`
- Fix branches: `fix/<issue-id-or-desc>`

## Commit Messages (Conventional Commits)
- `feat:` new capability
- `fix:` bug fix
- `docs:` documentation only
- `chore:` maintenance
- `refactor:` internal reorganization
- `perf:` performance improvement
- `test:` adds/updates tests

## Pull Requests
1. Keep PRs focused
2. Include before/after screenshots for UI changes
3. Ensure build passes

## Testing
(Placeholder; add unit/instrumentation tests.)
